import {decorate, observable} from "mobx";

// App's main state

class StateStore {

    constructor() {
    } // constructor

} // class StateStore

decorate(StateStore, {
});

export default new StateStore();
